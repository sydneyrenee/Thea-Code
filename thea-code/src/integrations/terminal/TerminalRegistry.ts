/**
 * Default timeout for terminal shell integration.
 */
export const TERMINAL_SHELL_INTEGRATION_TIMEOUT = 5000;

/**
 * Manages terminal integration for the Thea Code extension.
 * This is a simplified version of the original implementation.
 */
export class TerminalRegistry {
  /**
   * Initializes the terminal registry.
   */
  public static initialize(): void {
    // In the full implementation, this would initialize the terminal registry
  }

  /**
   * Cleans up the terminal registry.
   */
  public static cleanup(): void {
    // In the full implementation, this would clean up the terminal registry
  }
}